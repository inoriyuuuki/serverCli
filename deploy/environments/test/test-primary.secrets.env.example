# =============================================================================
# ServerCLI 测试环境 - 主节点 Secret 占位模板（test-primary）
#
# ⚠️  仅部署机使用，权限必须为 0600，不得提交真实值到 Git！
# 复制为 test-primary.secrets.env 后填入真实值；scripts 会自动 chmod 600。
# 脚本只从本文件/运行时环境读取 Secret，绝不写入日志、命令行参数或仓库。
# =============================================================================

# 管理员初始密码（运行时注入，首次初始化后即失效；留空则由 bootstrap-admin.sh 交互式输入）
ADMIN_INITIAL_PASSWORD=
# 或改为指向 0600 密码文件（与上面二选一）：
# ADMIN_INITIAL_PASSWORD_FILE=/path/to/admin-password

# SQLite 可留空（默认 file:<AGENT_STATE_DIR>/servercli.db）；若需覆盖：
# DATABASE_URL=file:/absolute/path/to/test-primary.db
