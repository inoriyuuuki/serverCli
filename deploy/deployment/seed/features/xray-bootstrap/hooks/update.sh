#!/usr/bin/env bash
# =============================================================================
# Hook: update — Xray Bootstrap (xray-bootstrap)
# 更新 = 幂等重装：install 会在版本变化时重新下载制品并重启。
# =============================================================================
set -euo pipefail
exec "$(dirname "$0")/install.sh" "$@"
